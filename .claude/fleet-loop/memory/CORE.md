# Fleet Loop Core Memory (Frontend)

This is the **small, durable** memory block — the only memory file `fleet-planner` and `fleet-qa-reviewer` read by default. It holds lessons that have proven recurring or load-bearing across runs.

Keep this short. A lesson is promoted here by `fleet-release-reporter` only after it has shown up in **2+ runs**. One-off observations stay in `archive/<date>-<slug>.md` and are not duplicated here until promoted.

## Watching (1 occurrence — not yet promoted)

_(none yet — this is the initial state)_

## Confirmed (2+ occurrences — treat as established process knowledge)

_(none yet)_

## Suggested CLAUDE.md additions (proposals for human review — not adopted)

_(none yet — proposals move here only once "Confirmed" above)_
