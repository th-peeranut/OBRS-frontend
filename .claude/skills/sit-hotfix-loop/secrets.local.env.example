# sit-hotfix-loop secrets — TEMPLATE (committed).
# Copy to `secrets.local.env` (gitignored) and fill in real values.
# The skill sources secrets.local.env; it never inlines secrets.

# --- SIT Supabase Postgres (for direct DDL / data migrations) ---
# Non-secret connection facts mirror OBRS-backend application-sit.yml.
SIT_DB_HOST=aws-1-ap-northeast-2.pooler.supabase.com
SIT_DB_PORT=5432                      # session mode — required for DDL (NOT 6543 txn pooler)
SIT_DB_NAME=postgres
SIT_DB_USER=postgres.ojgxvmzduvlasnyzjhjt
SIT_DB_PASSWORD=__FILL_ME__

# --- SIT admin app account (for post-deploy verification of admin-gated flows) ---
SIT_ADMIN_EMAIL=admin@system.local
SIT_ADMIN_PASSWORD=__FILL_ME__

# --- Issue tracker: GitHub Issues via `gh` CLI (run `gh auth login`; no secret stored here) ---
# Repo: th-peeranut/OBRS-backend

# --- Jira Cloud API (attachments; no MCP tool covers file uploads) ---
# Generate at https://id.atlassian.com/manage-profile/security/api-tokens
# ("Create API token", not the scoped variant — this uses classic Basic Auth).
JIRA_SITE=nj-phuyaipu.atlassian.net
JIRA_EMAIL=__FILL_ME__
JIRA_API_TOKEN=__FILL_ME__
